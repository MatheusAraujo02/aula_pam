import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
        backgroundColor: '#EEE',
        padding: 8,
        alignItems:'center',
        borderRadius: 20,
    },
    titulo: {
        margin: 6,
        fontSize: 30,
        fontWeight: 'bold',
        color: '#AAA'
    },
})

export default styles;